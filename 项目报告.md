# AI 接口自动化测试 Agent — 项目报告

---

## 一、项目需求

### 1.1 背景

在微服务架构日益普及的今天，API 接口测试是保障服务质量的关键环节。传统的手工编写测试用例方式效率低、覆盖率不足，无法满足快速迭代的需求。

### 1.2 目标

构建一个基于 AI 的接口自动化测试 Agent，实现以下核心能力：

- **自动发现接口**：输入被测系统地址，自动抓取并解析 OpenAPI/Swagger 文档
- **AI 生成用例**：调用大语言模型为每个接口自动生成正常、异常、边界三类测试用例
- **自动执行验证**：发送真实 HTTP 请求执行测试，AI 校验返回结果是否符合预期
- **可视化报告**：生成 HTML 可视化测试报告，展示通过率与失败详情

### 1.3 分布式系统要求

项目需满足以下分布式系统特征：

| # | 要求 | 说明 |
|---|------|------|
| 1 | 至少 3 个微服务 | 服务拆分，独立部署 |
| 2 | 跨服务调用 | 服务间通过 HTTP 通信 |
| 3 | 服务注册与发现 | 动态感知服务上下线 |
| 4 | 负载均衡 | 请求在实例间合理分配 |
| 5 | 服务容错与降级 | 部分组件故障时系统仍可用 |
| 6 | 容器化部署 | Docker 编排，一键启动 |
| 7 | 数据库分库 | 每服务独立数据库 |
| 8 | 健康检查 | 每个服务暴露健康状态 |
| 9 | 配置中心 | 配置统一管理，支持动态变更 |
| 10 | API 文档 | 每个服务提供接口文档 |

---

## 二、系统架构

### 2.1 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         用  户                                  │
│                  http://localhost:8080                           │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│              task-scheduler (:8080)                              │
│              任务调度服务 — 统一入口 + 工作流编排                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐    │
│  │ 创建任务  │  │ 启动流程  │  │ 查询进度  │  │ 获取报告      │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────┘    │
└──────┬──────────────┬──────────────┬────────────────────────────┘
       │              │              │
       │ HTTP (httpx) │              │
       ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────────┐
│ api-scanner  │ │ ai-planner   │ │ test-executor    │
│ (:8081)      │ │ (:8082)      │ │ (:8083)          │
│              │ │              │ │                  │
│ 接口扫描服务  │ │ AI规划服务   │ │ 测试执行与报告    │
│              │ │              │ │                  │
│ · 探测OpenAPI│ │ · AI生成用例  │ │ · HTTP请求执行   │
│ · 解析接口   │ │ · 正常/异常/ │ │ · AI结果校验     │
│              │ │   边界覆盖   │ │ · HTML报告生成   │
│   [SQLite]   │ │   [SQLite]   │ │   [SQLite]       │
└──────────────┘ └──────────────┘ └──────────────────┘
       │              │              │
       └──────────────┼──────────────┘
                      │ 注册/发现/配置
                      ▼
          ┌───────────────────┐
          │  Nacos (:8848)    │
          │  注册中心+配置中心  │
          └───────────────────┘
```

### 2.2 调用链路

```
用户 → Task Scheduler
         │
         ├─ Step 1: POST /api/v1/scan    → api-scanner  → 返回接口列表
         │
         ├─ Step 2: POST /api/v1/plans   → ai-planner   → 返回测试用例
         │
         └─ Step 3: POST /api/v1/execute → test-executor → 返回执行报告
```

### 2.3 微服务拆分策略

采用**按业务能力拆分**（Decompose by Business Capability）：

| 服务 | 端口 | 职责 | 拆分理由 |
|------|------|------|----------|
| task-scheduler | 8080 | 任务管理 + 工作流编排 | 统一入口，协调调用顺序，不执行具体业务逻辑 |
| api-scanner | 8081 | OpenAPI 文档抓取与解析 | 独立于下游，可单独用于接口发现场景 |
| ai-planner | 8082 | AI 生成测试用例 | AI 调用耗时较长（60-90s），独立部署避免阻塞其他服务 |
| test-executor | 8083 | 执行测试 + AI 校验 + 报告 | 计算密集型（批量 HTTP 请求 + AI 校验），需独立伸缩 |

**拆分原则**：单一职责、独立数据库、HTTP API 通信、独立部署。

---

## 三、技术选型

| 技术领域 | 选型 | 理由 |
|----------|------|------|
| 编程语言 | Python 3.13 | 开发效率高，AI/HTTP 生态成熟 |
| Web 框架 | FastAPI | 异步高性能，自动生成 Swagger 文档，类型安全 |
| 数据库 | SQLite + aiosqlite + SQLAlchemy 2.0 | 轻量级，零配置，每服务独立 DB 文件，异步 ORM |
| 服务通信 | httpx + ServiceClient（类似 RestTemplate） | 手写轻量级跨服务调用客户端，封装 Nacos 服务发现 + 客户端负载均衡 + 静态配置降级 |
| 服务发现 | Nacos (HTTP API 手写封装) | Java 生态标准注册中心，提供 REST API 可直接调用 |
| 配置中心 | Nacos Config | 统一配置管理，不可用时降级环境变量 |
| AI 引擎 | DeepSeek（OpenAI 兼容 SDK）+ Anthropic Claude SDK 双通道 | 主力使用 DeepSeek，预留 Claude 切换能力，通过环境变量配置 |
| 容器化 | Docker + Docker Compose | 一键编排 5 个容器，单机模拟分布式环境 |
| 前端 | 原生 HTML + CSS + JS | 轻量单页面，无需额外构建工具 |

---

## 四、部署说明

### 4.1 环境要求

- **Docker 方式**：Docker + Docker Compose
- **本地方式**：Python 3.13

### 4.2 Docker Compose 部署（推荐）

```bash
# 1. 配置环境变量
cp .env.example .env
# 编辑 .env，填入 AI_API_KEY

# 2. 一键启动
docker compose up -d

# 3. 访问
# 前端页面:    http://localhost:8080/
# Swagger文档: http://localhost:8080/docs
# Nacos控制台: http://localhost:8848/nacos/ (nacos/nacos)
```

5 个容器：Nacos + task-scheduler + api-scanner + ai-planner + test-executor

### 4.3 本地开发部署

```bash
# 1. 安装依赖
pip install fastapi uvicorn sqlalchemy aiosqlite httpx

# 2. 开4个终端分别启动
python -m task-scheduler.main    # :8080
python -m api-scanner.main       # :8081
python -m ai-planner.main        # :8082
python -m test-executor.main     # :8083
```

> 注意：必须使用 `python -m` 方式启动，因为代码使用了相对导入 `from . import config`。
> Nacos 不启动也不影响核心功能，服务会自动降级到 .env 中的静态地址配置。

---

## 五、关键技术难点与解决方案

### 5.1 Docker 容器内相对导入失败

**问题**：Docker 内代码平铺导致 `from . import config` 找不到父包。

**解决**：保持包目录结构，改用 `python -m task_scheduler.main` 启动（连字符改下划线）。

### 5.2 AI 输出格式不可控

**问题**：大模型输出被 markdown 包裹、含 JS 表达式（`"x".repeat(10)`）、或被 token 限制截断，导致 JSON 解析失败。

**解决**：Prompt 约束纯 JSON 输出 + `_repair_json()` 正则修复 JS 表达式 + `_repair_truncated()` 修复截断 JSON + 多层回退解析。分批处理（`BATCH_SIZE=5`）避免 token 超限。

### 5.3 Prompt 模板大括号冲突

**问题**：Prompt 含大量 JSON 示例，`.format()` 时 `{}` 被当作占位符。

**解决**：改用 `.replace()` 做字符串替换，互不干扰。

### 5.4 Nacos 不可用时的容错降级

**问题**：Nacos 故障时服务发现失败，跨服务调用中断。

**解决**：`ServiceClient` 和 `ConfigCenter` 均实现双重降级链（Nacos → 静态配置/环境变量），心跳自动重连，保证 Nacos 完全不可用时核心功能正常。

### 5.5 工作流异步编排

**问题**：下游服务耗时数十秒至数分钟，不能阻塞等待。

**解决**：异步创建 + 轮询模式 — POST 创建任务后每 1 秒轮询状态，完成后拉取结果，前端可实时查询进度。

---

## 六、运行情况

### 6.1 服务启动

4 个微服务均正常启动，Nacos 注册成功，健康检查全部通过：

| 服务 | 端口 | 健康检查 | Nacos 注册 |
|------|------|----------|------------|
| task-scheduler | 8080 | `/health` → ok | 已注册 |
| api-scanner | 8081 | `/health` → ok | 已注册 |
| ai-planner | 8082 | `/health` → ok | 已注册 |
| test-executor | 8083 | `/health` → ok | 已注册 |

### 6.2 端到端流程验证

完整自动化测试流程跑通：创建任务 → 扫描接口 → AI 生成用例 → 执行测试 → 生成报告。

- 被测系统 5 个端点 → AI 生成 15 条测试用例（正常/异常/边界各 5 条）
- 测试报告输出至 `reports/report.html`，包含通过率统计和失败详情

### 6.3 Git 版本管理

| 项目 | 内容 |
|------|------|
| 远程仓库 | https://github.com/Yuuko-06/auto-test.git |
| 分支 | main |
| 提交次数 | 3 次 |

**提交记录：**

| 提交 | 说明 | 变更 |
|------|------|------|
| `aa23c7c` | feat: AI接口测试，分布式微服务 | 48 个文件，4397 行（项目初始化） |
| `6d3764c` | 更新代码 | 10 个文件，254 行（配置中心 + Nacos Config 降级） |
| `538d490` | fix: 修复Docker容器内相对导入失败 + Nacos端口冲突 | 6 个文件，17 行 |

---

## 七、项目分工

本项目为个人独立完成，承担全部角色：

| 角色 | 工作内容 |
|------|----------|
| 架构设计 | 微服务拆分方案、通信协议设计、数据模型定义 |
| 后端开发 | 4 个微服务的 API 路由、业务逻辑、数据库层 |
| 基础设施 | Nacos 注册中心/配置中心接入、Docker Compose 编排 |
| 共享库开发 | ServiceClient、NacosClient、ConfigCenter、数据库基础模块 |
| AI 集成 | DeepSeek + Claude 双通道 SDK 封装、Prompt 工程、JSON 修复 |
| 前端开发 | 单页面任务管理界面 |
| 测试验证 | 端到端流程验证、边界场景测试 |

---

## 八、收获与改进方向

### 8.1 技术收获

- **微服务全链路实战**：从零搭建分布式系统，覆盖服务拆分、注册发现、配置中心、降级容错，没有照搬 Spring Cloud，针对 Python 生态手写了轻量级替代方案
- **AI 工程化**：将大模型嵌入自动化流程，积累 Prompt 工程、输出容错修复的实战经验
- **容器化部署**：掌握 Docker Compose 多服务编排，理解容器内外网络、文件系统的差异

### 8.2 思维收获

- **降级优先设计**：从一开始就考虑组件故障场景，而非事后补救
- **宽容解析**：AI 输出不可控，与其追求完美输出，不如做好解析层的容错修复

### 8.3 改进方向

- **消息队列解耦**：引入消息队列替代轮询，下游完成后主动通知，降低延迟
- **API 网关 + 链路追踪**：统一鉴权、限流、日志，集成 OpenTelemetry 可视化调用链
- **分布式锁与幂等**：多实例部署时防止任务重复执行，保证接口幂等
- **CI/CD**：GitHub Actions 自动构建镜像并推送
